// src/lib/theme/suburbThemes.ts
// Keep minimal — hook for future customisations per suburb
import type { SuburbTheme } from './tokens';

export const suburbThemes: Record<string, SuburbTheme> = {
  // 'springfield-lakes': { accent: { a: '#10b981' } },
};
